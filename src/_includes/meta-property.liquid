{% assign propertyTitle = site.meta.title | default: '' %}
{% if propertyTitle == '' %}
  {% if site.name == page.title %}
    {% assign propertyTitle = page.title %}
  {% else %}
    {% assign propertyTitle = page.title | append: ' :: ' | append: site.name %}
  {% endif %}
{% endif %}
{% assign propertyUrl = site.url | append: page.url %}

<meta property="og:site_name" content="{{ site.name }}" />
<meta property="og:title" content="{{ propertyTitle }}" />
<meta property="og:type" content="website" />
<meta property="og:url" content="{{ propertyUrl }}" />
