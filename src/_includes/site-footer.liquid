<footer class="sticky bottom-0">
  <nav class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <ul class="list-unstyled flex h-16 items-center gap-3">
      {% for item in navigation.footer %}
        {% assign current = '' %}
        {% if item.url == page.url %}
          {% assign current = 'aria-current="page"' %}
        {% endif %}
        {% assign separatorClasses = '' %}
        {% unless forloop.first %}
          {% assign separatorClasses = 'before:block before:h-1 before:w-1 before:rounded-full before:bg-slate-400' %}
        {% endunless %}
        <li class="flex items-center gap-3 {{ separatorClasses }}">
          <a
            href="{{ item.url }}"
            class="text-sm md:text-base"
            {{ current }}
          >
            {{ item.text }}
          </a>
        </li>
      {% endfor %}
    </ul>
  </nav>
</footer>
